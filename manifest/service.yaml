apiVersion: v1
kind: Service
metadata:
  name: receiverservice-service
  namespace: receiver
spec:
  type: NodePort
  selector:
    app: receiverservice
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 30081